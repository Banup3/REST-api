# This is a root .env file for quick reference
# Copy backend/.env.example to backend/.env and configure

# Quick Start Configuration:
# 1. cd backend && cp .env.example .env
# 2. Update MongoDB URI and JWT secrets
# 3. npm run dev
